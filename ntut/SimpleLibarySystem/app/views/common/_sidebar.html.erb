<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
            <%= link_to "Lib", books_url, class:"navbar-brand" %>
        </div>
    <div>
        <ul class="nav navbar-nav  navbar-right">
            <% if current_user %>
                <li><%= link_to('登出', destroy_user_session_path, :method => :delete) %></li>
                <li><%= link_to('修改資料', edit_registration_path(:user)) %></li>
                <% if current_user.has_role? :admin %>
                    <li><%= link_to('管理使用者', admin_users_url) %></li>
                <% end %>
                <% if current_user.has_role? :librarian %>
                    <li><%= link_to('查看還書', librarian_receipts_url) %></li>
                    <li><%= link_to('管理預約', librarian_bills_url) %></li>
                <% end %>
                <li><%= link_to('借閱現況', user_url(current_user)) %></li>
            <% else %>
                <li><%= link_to('登入', new_session_path(:user)) %></li>
            <% end %>
        </ul>
    </div>
</nav>
